<!--index.wxml-->
<scroll-view class="container" scroll-y>
  <!-- 标题区域 -->
  <view class="header">
    <view class="header-content">
      <text class="app-title">🔍 烧屏检测助手</text>
      <text class="app-desc">🏷️ 专业屏幕检测工具</text>
    </view>
  </view>

  <!-- 主要功能按钮区域 -->
  <view class="main-actions">
    <view 
      class="action-btn" 
      wx:for="{{mainActions}}" 
      wx:key="action"
      data-action="{{item.action}}"
      bindtap="handleMainAction"
      style="border-color: {{item.color}}"
    >
      <text class="action-icon">{{item.icon}}</text>
      <view class="action-content">
        <text class="action-title">{{item.title}}</text>
        <text class="action-desc">{{item.desc}}</text>
      </view>
    </view>
  </view>

  <!-- 信息流广告位（在检测入口之间） -->
  <!-- <view class="ad-banner-inline" wx:if="{{false}}">
    <view class="ad-label">广告</view>
    <view class="ad-content" bindtap="onAdClick">
      <text class="ad-text">推荐：专业屏幕检测服务，了解更多 →</text>
    </view>
  </view> -->

  <!-- 菜单项区域（已移除：我的设备、检测记录、使用指南） -->
  <!-- <view class="menu-section" wx:if="{{false}}">
    <view 
      class="menu-item" 
      wx:for="{{menuItems}}" 
      wx:key="action"
      data-action="{{item.action}}"
      bindtap="handleMenuClick"
    >
      <text class="menu-icon">{{item.icon}}</text>
      <text class="menu-title">{{item.title}}</text>
      <text class="menu-arrow">></text>
    </view>
  </view> -->

  <!-- 底部横幅广告 -->
  <!-- <view class="ad-banner-bottom" wx:if="{{false}}">
    <view class="ad-label">广告</view>
    <view class="ad-banner-content" bindtap="onAdClick">
      <text class="ad-banner-text">广告位：480×200px</text>
      <text class="ad-banner-desc">点击查看详情</text>
    </view>
  </view> -->
</scroll-view>
